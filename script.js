



const gameBoard = (() => {
    const board = ["", "", "", "", "", "", "", "", ""];
    return {
        board
    }
})();

const player = (name) => {
    const move = () => console.group(move + name)
}